import { checkFormText } from "../src/client/js/checkFormText"

describe("Testing the input functionality", () => {
    test("Testing the checkFormText() function", () => {
           expect(checkFormText).toBeDefined();
    })
});
